version: '3.8'

services:
  db:
    image: postgres:15
    container_name: madeireira_db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: '@K1a2y3k4'
      POSTGRES_DB: madeireira_erp
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: